# Copyright 2026 Canonical Ltd.
# See LICENSE file for licensing details.

name: Terraform modules compliance

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "terraform/**"

jobs:
  compliance-check:
    uses: canonical/operator-workflows/.github/workflows/terraform_modules_compliance.yaml@428b02d2dbe42238177aeaf4b9d3488e068e41b9 # main
    with:
      terraform-directories: '["terraform/modules/gateway-api-integrator", "terraform/product"]'
