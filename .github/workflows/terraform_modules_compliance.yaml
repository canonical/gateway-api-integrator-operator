# Copyright 2026 Canonical Ltd.
# See LICENSE file for licensing details.

name: Terraform modules compliance

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "terraform/**"

jobs:
  cc008:
    uses: canonical/operator-workflows/.github/workflows/terraform_modules_compliance.yaml@94c3a0c54ca84d82351c419a77f3f61d56b478e8 # main
    with:
      terraform-directories: '["terraform/modules/gateway-api-integrator", "terraform/product"]'
