name: Publish the Gateway API Integrator charm to edge

on:
  workflow_dispatch:
  push:
    branches:
    - main
    paths:
    - 'gateway-api-integrator/**'

jobs:
  publish-to-edge:
    uses: canonical/charm-ci/.github/workflows/publish-artifacts.yml@v0.0.1-alpha.8
    permissions:
      actions: read
      contents: write
    secrets:
      CHARMHUB_TOKEN: ${{ secrets.CHARMHUB_TOKEN }}
    with:
      integration-workflow-name: integration_test.yaml
      working-directory: "."
